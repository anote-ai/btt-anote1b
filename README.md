# Anote monorepo

- **`Personal/`** — FastAPI leaderboard (Vite frontend, pytest). This is the primary app that used to live at the repo root.
- **`Company/`** — Company-facing Flask API + Create React App landing (see `Company/CLAUDE.md`).

Local clones: work inside `Personal/` or `Company/` per their READMEs / CLAUDE.md.
