# Copy to .env and adjust locally (see root .gitignore).
DATABASE_URL=sqlite:///./leaderboard.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/leaderboard

# Tests only (see tests/conftest.py)
# DISABLE_RATE_LIMIT=1

# Optional: init_db.py
# FORCE_RESEED=false
